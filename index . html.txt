<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Quranic Online Class Registration</title>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: Arial, sans-serif;
      height: 100vh;
      overflow: hidden;
      background: linear-gradient(to right, #0f172a, #134e4a);
      display: flex;
      justify-content: center;
      align-items: center;
      position: relative;
    }

    /* Flying stars */
    .star {
      position: absolute;
      width: 6px;
      height: 6px;
      background: white;
      border-radius: 50%;
      opacity: 0.8;
      animation: fly 10s linear infinite;
    }

    .star:nth-child(1) { left: 5%; animation-duration: 8s; }
    .star:nth-child(2) { left: 20%; animation-duration: 12s; }
    .star:nth-child(3) { left: 35%; animation-duration: 9s; }
    .star:nth-child(4) { left: 50%; animation-duration: 11s; }
    .star:nth-child(5) { left: 65%; animation-duration: 7s; }
    .star:nth-child(6) { left: 80%; animation-duration: 13s; }
    .star:nth-child(7) { left: 92%; animation-duration: 10s; }

    @keyframes fly {
      0% {
        transform: translateY(100vh) scale(1);
        opacity: 0;
      }
      20% {
        opacity: 1;
      }
      100% {
        transform: translateY(-120vh) scale(1.8);
        opacity: 0;
      }
    }

    .form-box {
      width: 90%;
      max-width: 450px;
      background: rgba(255, 255, 255, 0.12);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255,255,255,0.2);
      padding: 35px;
      border-radius: 20px;
      color: white;
      z-index: 10;
      animation: floatCard 4s ease-in-out infinite;
      box-shadow: 0 10px 30px rgba(0,0,0,0.4);
    }

    @keyframes floatCard {
      0%,100% {
        transform: translateY(0px);
